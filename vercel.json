{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter metricmint-app run build && pnpm build:api",
  "outputDirectory": "artifacts/metricmint/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/(.*)",        "destination": "/index.html" }
  ]
}
